{
  "name": "threadhop-site",
  "private": true,
  "type": "module",
  "description": "threadhop.parzival.computer — landing page for ThreadHop",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "marked": "^18.0.12"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
